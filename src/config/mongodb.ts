// MongoDB configuration file
// This file will be used to connect to MongoDB from the backend

export const mongodbConfig = {
  // Backend will read these from environment variables
  // MONGODB_URI: process.env.MONGODB_URI || 'mongodb://localhost:27017/intellilaw'
  // Environment variables should be set in .env file
};

// This configuration will be used by the Express backend
// Frontend will communicate with backend via REST API
